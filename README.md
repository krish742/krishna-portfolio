# Krishna Pandya — UX Research & UX Design Portfolio

Official portfolio website for **Krishna Pandya**, a UX Researcher and UX Designer based in Ontario, Canada, creating accessible, research-driven, human-centered digital experiences.

---

## 🛠️ Technology Stack

- **Framework**: TanStack Start (`@tanstack/react-start` v1.168, `@tanstack/react-router` v1.170)
- **UI & Components**: React 19 (`react` v19.2), Radix UI primitives, Lucide React icons
- **Styling & Aesthetics**: Tailwind CSS v4 (`tailwindcss` v4.2), Custom dark visual design system
- **Build Engine & SSR**: Vite 8 (`vite` v8.1), Nitro SSR v3 (`nitro` v3.0)
- **Language**: TypeScript v5.8 (`typescript` v5.8)
- **Code Quality**: ESLint v9, Prettier v3

---

## 🚀 Quick Start

### Prerequisites

- **Git**
- **Node.js**: v18.18+ or v20.x+ (v20 LTS recommended)
- **npm**: v9.x or v10.x

### Local Setup Instructions

1. **Clone the Repository**:

   ```bash
   git clone https://github.com/Nisargpandya07/empathetic-spaces-main.git
   cd empathetic-spaces-main
   ```

2. **Install Dependencies**:

   ```bash
   npm install
   ```

3. **Start Development Server**:
   ```bash
   npm run dev
   ```
   Open `http://localhost:3000` in your web browser.

---

## 📋 Verification & Build Commands

Before committing changes or pushing code, run these verification commands:

- **Format Check / Auto-format**:
  ```bash
  npm run format
  ```
- **TypeScript Typecheck**:
  ```bash
  npx tsc --noEmit
  ```
- **ESLint Code Quality**:
  ```bash
  npm run lint
  ```
- **Production Build**:
  ```bash
  npm run build
  ```

---

## 🤝 Repository Collaboration & Git Workflow

- **Syncing before work**: `git pull origin main`
- **Committing changes**: `git status` ➔ `git add .` ➔ `git commit -m "Description"` ➔ `git push origin main`
- **Branching for major updates**: Use `git checkout -b feature-branch-name` for multi-step changes.
- **Rule**: Do **NOT** force-push to `main`.

For detailed setup, troubleshooting, and project structure, refer to [SETUP.md](./SETUP.md).

---

## 🔒 Content & Authenticity Rules

- **No Fabrication**: Do not invent fake client projects, fake usability metrics, or fictional approvals.
- **Authentic Details**: Preserve Krishna's real contact info (`krishnapandya391@gmail.com`, `548-333-2167`, LinkedIn).
- **Real Artifacts**: Use authentic screenshots and proof cards from `src/assets/`.
- **Independent UX Practice**: Keep self-directed practice explicitly labeled (`"Self-Directed Practice • Not Client Work"`).
- **Internal Handoff Folder**: `KRISHNA_PORTFOLIO_FINAL_MASTER_HANDOFF/` and `scratch_handoff.txt` are internal reference materials kept strictly local via `.gitignore`. Do not commit or push them.
